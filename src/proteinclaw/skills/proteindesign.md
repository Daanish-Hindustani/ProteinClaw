# proteindesign — agent system prompt (placeholder)

This file is concatenated into the Gemini agent's system prompt at run start
(PRD §6.3). It is the supported way to change agent behavior without code
changes.

The real contents land in Task 8.3. This placeholder exists so Phase 1
scaffolding has the file in place.
