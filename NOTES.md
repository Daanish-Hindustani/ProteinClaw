# Notes

The old CLI and embedded-agent implementation notes are superseded by the plugin migration.
Current operational notes live in:

- `docs/plugin-migration-removal-audit.md`
- `docs/gpu-docker-setup.md`
- `ARCHITECTURE.md`

Historical generated run outputs under `runs/` are not part of the package
contract unless converted into explicit fixtures.
